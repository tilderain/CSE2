{
    "tasks": [
        {
            "type": "shell",
            "label": "execute make",
            "command": "cmake --build build",
            "args": [

            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}